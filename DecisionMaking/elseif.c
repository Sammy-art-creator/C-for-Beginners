#include <stdio.h>

int main() {
    int age;
    scanf("%d", &age);
    // Don't touch above this line
    // Write code here
    if (age > 18) {
    printf("Above 18");
    }
    else {
    printf("Below or equal 18");
    }
    return 0;
};